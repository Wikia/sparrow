web:
  build: .
  command: python apps/manage.py runserver 0.0.0.0:8000
  volumes:
   - .:/code
  links:
   - db
  ports:
   - "8000:8000"
  environment:
   POSTGRES_PASSWORD: "foo"
   SPARROW_SECRET_KEY: ""
db:
  image: postgres